import { ReactElement } from 'react';

export default function Card () : ReactElement {
    return (
        <div className="card">
            <div className="header">
                <span>卡片 Card</span>
            </div>
        </div>
    )
}
